#pragma once
#include <H3/H3_Board.h>

//struct H3_Game;

struct H3_DemoTileRenderer
{
    de::VideoDriver* m_driver;
    de::Shader* m_shader;
    uint32_t m_texture2DArray;
    uint32_t m_vao;
    uint32_t vbo_instanceMat;
    uint32_t vbo_instanceLayer;
    uint32_t vbo_instanceAngle60;
    std::vector<glm::mat4> m_instanceMat;
    std::vector<uint8_t> m_instanceLayer;
    std::vector<uint8_t> m_instanceAngle60;
    H3_DemoTileRenderer();
    ~H3_DemoTileRenderer();
    
    void init(de::VideoDriver* driver);
    
    // Function to generate gradient textures
    void drawGradient(uint8_t* data, int w, int h, int layer,
                      uint8_t r1, uint8_t g1, uint8_t b1,
                      uint8_t r2, uint8_t g2, uint8_t b2);

    void render( glm::mat4 const & projViewMat );
};
